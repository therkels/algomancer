## Starting up with poetry
1. pip install poetry
2. poetry self add poetry-plugin-shell
3. poetry isntall
4. poetry shell

## Configuration
To set a configuration value, use the following command:
```
algom set-config --key <key> --value <value>
```